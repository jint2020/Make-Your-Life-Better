# Make Your Life Better — 设计摘要

> 2026-09-23 经过 grilling 访谈确认；2026-09-24 追加部署与后端（第三节）。改动设计时先更新这份文档。

## 一、整体架构

- **定位**：浏览器里使用的提效工具集。**默认数据不离开本机**：解析和计算只在浏览器里做；只有用户登录后主动选择"保存到云端"的任务才会上传（见第三节）
- **技术栈**：Vite + React 19 + TypeScript strict + Tailwind v4 + shadcn + tweakcn + React Router 8 + pnpm
- **结构**：单页应用（SPA），每个工具一个懒加载路由
  - `src/tools.registry.ts` 统一登记工具的元信息和路由
  - 首页是工具卡片网格，顶部导航可以切换工具
  - 共享能力放在 `src/shared/`：Worker 解析、IndexedDB 存储、上传组件
- **主题**：内置几套 tweakcn 预设，用户在设置里切换，选择存在 localStorage
  - 支持深色/浅色（跟随系统，也可以手动切换）
  - AG Grid 通过 Theming API 读同一组 CSS 变量
  - 差异、缺失、重复这几个语义色单独定义，只跟深浅色走，不跟用户主题走
- **部署**：docker compose（Caddy + FastAPI + PostgreSQL + MinIO），见第三节
- **语言**：界面只做中文，文案集中管理

## 二、工具一：Excel 数据对比

### 输入

- 支持 xlsx、csv，**不支持 xls**；一次 2～3 个文件
- xlsx 用 SheetJS 0.20.3（从官方 CDN 的 tgz 安装，不用 npm 上的 0.18.5），csv 用 PapaParse，都在 Web Worker 里跑
- CSV 编码自动检测：先用严格 UTF-8 解码，失败就换 GB18030；也可以手动切换，并预览前几行
- 用户可以选 sheet、指定表头行，合并单元格自动向下填充

### 对比模型

- 按主键匹配（支持组合主键），再比较选中的字段
- 同名列自动配对，不同名的由用户手动映射
- **无基准宽表**：每行可以同时带多个标签，包括一致、有差异、某文件缺失
- 主键重复、主键为空的行单独归一类，不参与对比，但会列出来

### 值归一化

- 默认规则：去掉首尾空格；空值都算相等；数字按数值比较（"1.0" = 1）；日期统一成 YYYY-MM-DD
- 开关：忽略大小写、全角转半角、文本型数字保留前导零（默认开，也就是 "001" ≠ "1"）

### 操作流程

- 第一次使用走向导：① 上传 → ② 选 sheet 和表头行 → ③ 选主键、对比字段并做映射 → ④ 看结果
  - ① 有文件后上传区收成一行，示例卡片隐藏；文件行显示行数（多个工作表时注明是哪个工作表的）
  - ② 每个文件默认折叠成一行摘要（工作表 · 表头第几行 · 表头前几列），点"调整"才展开预览；出错或表头行是空的时自动展开
  - ③ 每个列下拉框下面显示这一列的几个示例值（Worker 解析表格时顺带取，不传整列数据）；"（无）"用虚线框和灰色斜体标出
- 到了结果页，配置收进侧边抽屉，改完点"重新对比"

### 结果展示

- 用 AG Grid Community，只读
- 列默认按"字段为主、文件为次"分组（同一字段的 A / B / C 挨在一起，最好比较）；可以切换成"文件为主"，选择记在本机。主键列、状态列固定在左边
  - 按字段并排时，列头只写 A / B / C，表格上方有图例说明各字母对应的文件
  - 某个文件没有映射的字段，那一列不显示（整列都是空的）
- 差异单元格高亮：3 个文件时只标出和多数不同的那个值；没有多数值（2 个文件，或 3 个各不相同）时都标。缺失的格子显示灰色"—"
- 状态列：有差异时直接写出是哪些字段不一样（如"部门 · 学分"），而不是只写"有差异"
- 顶部汇总条：全部一致 / 有差异 / 有缺失 / 主键重复 / 主键为空，点击可以筛选（一行可能同时属于多类）。默认只显示有差异的行
- 附带展示列：只展示，不参与比较
- 结果页把标题和步骤条压成一行，把高度留给结果表
- 行详情抽屉：一行数据的"字段 × 文件"竖表
- "只显示有差异的列"开关
- 主键搜索框

### 导出

- 导出 xlsx，差异单元格带底色，和页面的高亮规则一致（3 个文件时只标不一样的那个）
- "对比结果" sheet：列的排列方式跟页面一致（按字段并排 / 按文件分组），两行表头（分组 + 列），冻结表头和主键、状态列，带自动筛选；缺失的格子写"—"并标灰
- 另外附一个"差异明细"长表 sheet：主键 / 字段 / 各文件的值，每处差异一行
- 有主键重复 / 为空的行时，再附一个"未参与对比的行" sheet
- 导出范围跟随当前筛选：行 = 当前筛选和排序后显示的行；列 = 当前的排列方式和"只显示有差异的列"
- 实现（2026-09-25 调整）：不用 ExcelJS。实测 10 万行 × 59 列要 198 秒、3.6 GB 内存，浏览器会崩。
  改成自写的最小 xlsx 生成器（共享字符串 + fflate 流式压缩），在 Worker 里生成：同样规模约 2.4 秒、37 MB。
  Worker 对比完保留一份结果，导出时主线程只传"显示了哪些行、什么顺序"，拿回文件字节

### 本地存储（IndexedDB）

- 存原始文件、配置和解析后的列式数据；结果不存，打开任务时重新算
- 最多 10 个任务，超过 30 天自动清理
- 有历史任务列表，可以单个删除、一键清空，并显示占用空间
- 首页提示"数据默认只保存在本浏览器"
- 无痕模式或存储空间满了的时候，降级为不保存

### 边界情况

- 表头空列名自动命名为"列C"，重名列改成"姓名(2)"
- 空 sheet 在第 ② 步拦住
- 文件损坏或加了密码时，显示具体原因
- 内存不够时提示用户拆分文件
- 主键映射不全时，第 ③ 步不能继续

## 三、部署与后端

> 2026-09-24 经过 grilling 访谈确认。

### 原则

- 不登录、后端不可用时，所有工具照常可用；账号和云端只是附加能力，访问不到后端时只隐藏云端入口
- 服务端只负责"存"，不负责"算"：不解析 Excel，不跑对比

### 仓库结构

- 同一个仓库，前后端各一个子目录：`web/`（现有前端整体挪入）、`server/`（FastAPI）
- 根目录放 `docker-compose.yml`（生产）、`docker-compose.dev.yml`（开发）、`docs/`、README、CLAUDE.md
- 接口契约：FastAPI 生成 OpenAPI，前端用 `openapi-typescript` 生成类型，调用用 `openapi-fetch`

### 后端技术栈

- Python + FastAPI，包管理用 uv
- SQLAlchemy 2.0（异步，asyncpg）+ Alembic；容器启动时自动执行 `alembic upgrade head`
- Pydantic v2 + pydantic-settings，配置全部来自环境变量
- MinIO 用 boto3 走 S3 协议（以后换云上对象存储只改 endpoint）
- 密码哈希 argon2，发信 aiosmtplib
- 代码检查和测试：ruff、pytest

### 服务拓扑（compose）

- `web`：Caddy + 前端构建产物（多阶段构建，一个镜像）。唯一对外的服务，开放 80/443，自动申请 HTTPS 证书
  - 提供静态文件，`/api/*` 转发给 `server`
  - 前后端同源，不需要跨域配置
- `server`：FastAPI
- `postgres`：账号、会话、验证码、任务元数据和配置
- `minio`：用户上传的原始文件
- `server`、`postgres`、`minio` 只在内部网络；MinIO 不对外，上传和下载都经过后端，先校验归属再转发
- 开发用的 compose 额外加 Mailpit

### 账号

- 只能用邮箱注册，注册必须通过邮箱验证码（证明邮箱属于本人）；注册时可以不设密码，之后在设置里补上
- 两种登录方式：邮箱 + 验证码；邮箱 + 密码（设过密码才能用）
- 不单独做忘记密码流程：用验证码登录后在设置里改密码
- 会话：服务端存会话，下发 httpOnly cookie（`SameSite=Lax`、`Secure`），可以退出，也能在服务端让会话失效
- 防滥用：同一邮箱 60 秒内只能发一次验证码，每天最多 10 次；验证码 6 位、10 分钟有效、最多试 5 次；密码连续输错临时锁定
- 发信：生产用通用 SMTP（参数全部来自环境变量，不绑定服务商）；开发用 Mailpit，E2E 从 Mailpit API 读验证码

### 云端保存（Excel 对比）

- 按任务手动保存：结果页有"保存到云端"按钮，点了才上传
- 只上传原始文件和配置；解析后的数据和结果不上传，打开时在浏览器里重新解析、重新计算
- 结构和本地 taskStore 对应：任务记录（配置）存 PostgreSQL，附件（原始文件）存 MinIO
- 历史列表分"本机"和"云端"两部分，每条标明存在哪里；云端任务可以下载到本机，也可以删除；不做自动同步
- 额度（环境变量可调）：每个用户总共 500 MB（`USER_QUOTA_BYTES`）、最多 50 个任务（`USER_MAX_TASKS`）、单个文件最大 50 MB（`MAX_FILE_BYTES`）
- 超出额度时提示先删旧任务；不自动过期；设置页显示已用和剩余空间
- 删除账号时，这个账号的所有云端文件一起删除
- 本地"最多 10 个、30 天清理"是缓存规则，和云端规则分开

### 构建和发布

- GitHub Actions：前端 lint / typecheck / test，后端 ruff / pytest，全部通过才构建 `web` 和 `server` 镜像并推送到 GHCR
- 镜像按 git tag 和 commit sha 打标签；服务器上执行 `docker compose pull && docker compose up -d`，回滚只改 `APP_VERSION`
- compose 里同时写 `image:` 和 `build:`，没有镜像仓库时也能在服务器上现场构建

### 本地开发

- 日常：开发用的 compose 只起 postgres、minio、mailpit；前端本机 `pnpm dev`，后端本机 `uv run fastapi dev`；Vite 用 proxy 把 `/api` 转发到本机后端（和生产一样同源）
- 上线前自测：一条命令用本机构建的生产镜像起全套服务（Caddy + server + postgres + minio + mailpit），验证 Caddy 配置和同源 cookie
- E2E 分两层：现有纯前端冒烟测试不依赖后端，保留；新增登录和云端保存的测试针对全套服务

### 备份

- **暂不做**（已知风险）：在补上之前，服务器磁盘损坏或服务器被删会丢失所有账号和云端文件

## 四、质量与性能

- 对比引擎写成纯函数，放在 Worker 里跑，用 Vitest 做单测，重点测值归一化
- 用生成的 10 万行测试文件跑 Playwright 冒烟测试
- 目标：3 个文件 × 10 万行 × 20 列，出结果 ≤ 5 秒；滚动保持 60fps；主线程上没有超过 100ms 的长任务

## 五、选型备忘

- 不用 Handsontable：7.0 之后只对非商业用途免费
- 不用 Glide Data Grid：正式版只支持到 React 18，支持 React 19 的版本还停在 alpha，维护也放缓了

## 六、实现进度（2026-09-23）

已完成：
- 解析：xlsx（SheetJS）/ csv（PapaParse）都在 Worker 里跑；合并单元格填充；表头行自动识别（跳过合并的大标题）；空列名 / 重名列处理；GBK 自动识别；拒绝 .xls 和加密文件
- 对比引擎：组合主键、主键重复 / 为空单独列出、逐字段比较、附带展示列；3 × 10 万行 × 20 列引擎耗时约 1.3 秒
- 向导四步、字段映射（同名自动配对；"工号 / 员工编号"这类主键列在候选唯一时自动配上）、比较规则开关
- 结果页：汇总条筛选、主键搜索、只显示有差异的列、行详情抽屉、调整配置抽屉、未参与对比的行列表
- 示例文件：一键生成 3 个带各种"坑"的文件，也用于 E2E 测试

实现中的补充决定：
- SheetJS 解析出的日期是 UTC 表示，统一转成本地时区的同一个钟面时间，避免东八区显示成 08:00
- 数字单元格如果显示为补零格式（000123），按显示文本读取

使用优化（2026-09-24 确认）：
- 字段映射表"用途"列的表头是一个"批量设置用途"下拉框，选项只有对比 / 只展示 / 忽略，作用于所有非主键字段；不做逐行勾选
- 主键只能逐行设置，批量操作不会改动主键
- 批量设为"对比"时，只在一个文件里出现的字段会跳过，保持原来的用途
- 批量设置后显示结果提示，并可撤销最近一次批量操作；之后只要逐行修改了字段配置，提示和撤销就失效
- 默认用途不变（公共字段默认对比）；字段搜索筛选、记住上次设置暂不做

2026-09-24 部署准备：
- 前端挪进 `web/`（仓库拆成 web/ + server/ 的第一步）
- `xlsx` 依赖改为和 lockfile 一致的 `@e965/xlsx@0.20.3`，保证 `--frozen-lockfile` 能装
- 后端骨架：FastAPI + 健康检查（存活 / 就绪）、SQLAlchemy 异步引擎、Alembic（容器启动时迁移）、MinIO 客户端（启动时自动建桶）
- 部署：生产 / 开发 / 本机自测三份 compose；Caddy 镜像（静态文件 + `/api` 转发 + 自动 HTTPS）；GitHub Actions 检查通过后推送镜像到 GHCR

实现中的补充决定（部署）：
- 官方 `minio/minio` 镜像已经不在 Docker Hub 上，改用社区维护的分支 `pgsty/minio`，固定版本，可以用 `MINIO_IMAGE` 替换
- Python 3.13

2026-09-24 账号和云端保存：
- 后端：邮箱验证码注册 / 登录、密码登录（设置密码后）、退出、修改密码（其他设备退出）、删除账号（连同云端文件）；验证码和密码的限流 / 锁定规则按第三节实现
- 云端任务接口：保存（multipart 上传原始文件 + 配置 JSON）、列表、详情、下载文件（经后端转发）、删除；额度检查时锁住用户行，防止并发上传同时通过
- 前端：顶栏账号入口、账号页（两种登录方式、已用空间、设置密码、退出、删除账号）；Excel 对比结果页"保存到云端"，上传步骤的"云端任务"列表（打开 / 删除）
- 打开云端任务：下载原始文件后自动走完解析、选工作表 / 表头行、字段配置、对比；表头和保存时不一样就停在字段步骤让用户确认
- 后端不可用（包括纯静态部署）时，账号和云端入口全部隐藏，工具照常使用
- 接口类型：后端导出 OpenAPI，前端用 openapi-typescript 生成类型、openapi-fetch 调用；CI 检查两者一致
- 全栈 E2E（`pnpm e2e:stack`）跑在生产镜像上，验证码从 Mailpit API 读取；暂不放进 CI

实现中的补充决定（账号 / 云端）：
- 会话 cookie 只在 `/api` 路径下发送；数据库只存 token 的 SHA-256
- 验证码只认最近一次发送的那个
- 邮件发送失败时这次不计入限流，可以立刻重试
- 本地历史任务（IndexedDB）的界面还没做，所以历史列表目前只有"云端"部分

2026-09-25 结果页可读性（优化，不是新需求）：
- 默认按字段并排、只标出不一样的那个文件、状态列写出差异字段、结果页顶部压缩成一行、隐藏没有映射的空列
- 对比引擎的 `diff[字段][行]` 从 0/1 改成按文件的差异掩码，结果里新增 `fieldFiles`（每个字段在哪些文件里有）

2026-09-25 中间两步减负（优化，不是新需求）：
- 上传步骤收起上传区、隐藏示例卡片、显示行数；选择工作表默认折叠成摘要；字段映射显示示例值、说明文字收进"用途说明"
- `TableSummary` 新增 `samples`（每列前 3 个不重复的非空值）

2026-09-25 导出 xlsx：
- 结果页"导出 Excel"：对比结果 + 差异明细 + 未参与对比的行，范围跟随当前筛选、排序和列设置
- 自写的最小 xlsx 生成器，在 Worker 里生成；10 万行 × 3 个文件全量导出约 2.1 秒、8.4 MB，主线程没有长任务
- 顺带修复：结果表用到的 AG Grid 行 API 模块（RowApiModule、ClientSideRowModelApiModule）之前没有注册

待做：
- IndexedDB 历史任务（存储层已完成，界面未接；也就是历史列表的"本机"部分）
- 备份（暂不做，已知风险）
