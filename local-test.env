# 只用于本机自测（docker-compose.local.yml），不要用在生产
APP_VERSION=local
SITE_ADDRESS=:80
POSTGRES_PASSWORD=local-test-password
MINIO_ROOT_PASSWORD=local-test-password
# 邮件都发到 Mailpit：http://localhost:8025
SMTP_HOST=mailpit
SMTP_PORT=1025
SMTP_SECURITY=none
SMTP_FROM=Make Your Life Better <no-reply@localhost>
